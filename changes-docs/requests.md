1. add browser persistence that remembers which arrows are white across sessions (i.e. closing, re-opening the tab)
2. add a small button beside the close contributor bar button that "switches" the side the contributor card sits on (default is right side, but pressing will move it to the left)
3. add a question mark button on the top right that is triggered by default once when the user opens the site. it gives a pop-up describing the functionality:
  - zoom, pan, rotate
  - pan with click shift or hold right-click
  - arrows are clickable with 4 cycle stages: on, leads to, pre-req, off
  - search functionality for contributors, topics
4. add a small reset button in the top left (beside the question mark button) that resets all arrows back to the default. enforce a confirmation pop-up box functionality to confirm the reset.